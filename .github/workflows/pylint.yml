name: Pylint

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10"]
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pylint
        - if [ -f reqyurenebts.txt ]; then python -m pip install -r requirements.txt
    - name: Analysing the code with pylint looking for errors
      run: |
        # break if errors in code exist
        pylint $(git ls-files '*.py') --errors-only
    - name: Analysing the code with pylint looking for warnings
      run: |
        pylint $(git ls-files '*.py') --exit-zero
